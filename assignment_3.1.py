import numpy as np
import matplotlib.pyplot as plt

N = [10, 100, 1000, 10000]
mean = np.array([-0.4, 1.1])
V = np.array([[1.0, 0.4], [0.4, 1.0]])

# TODO: Sample from multivariate normal \mathcal{N}(mean, V) based on N and compute mean and V via Monte Carlo for different N
# 	Only compute one diagonal and one off-diagonal value for V since symmetry.
#	Computing onlynone mean value is also fine.





# TODO (optional): Plot means and cov for increasing number of samples (optional)




# TODO: Plot errors over increasing number of samples

